import { User } from '@repo/types'
import React from 'react'

const page = () => {
  const test: User = { id: "1", email: "aa", name: "demo" }
  return (
    <div>page</div>
  )
}

export default page